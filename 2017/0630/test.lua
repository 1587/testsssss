#!/usr/local/bin/lua
local arr = {'3.8.9',' '}


-- local verInfo = {'3.8.9','20170629','user','od'}

-- local verMainInfo = string.split(table.remove(verInfo, 1), '_')
-- local verMain = toolkit.to_string(table.remove(verMainInfo, 1))
-- verMain=3.8.9
-- local verMinor = toolkit.to_string(table.remove(verMainInfo, 1))
-- verMinor=''
-- local verDate = toolkit.to_int(table.remove(verInfo, 1))
-- 日期
-- local verExtra = toolkit.to_string(table.concat(verInfo, '-'))
-- 其他

--      table.remove(arr,1)

--      for k,v in ipairs(arr) do
--             print(v)
--         end

local a = '3.6.9.1'
local b =  '3.6.8'

if( a > b ) then
    print(1)
else
    print(2)
end

-- local verInfo = {'3.8.9','20170629','user','od'}

-- local verMainInfo = string.split(table.remove(verInfo, 1), '_')
-- local verMain = toolkit.to_string(table.remove(verMainInfo, 1))
-- verMain=3.8.9
-- local verMinor = toolkit.to_string(table.remove(verMainInfo, 1))
-- verMinor=''
-- local verDate = toolkit.to_int(table.remove(verInfo, 1))
-- 日期
-- local verExtra = toolkit.to_string(table.concat(verInfo, '-'))
-- 其他

--      table.remove(arr,1)

--      for k,v in ipairs(arr) do
--             print(v)
--         end